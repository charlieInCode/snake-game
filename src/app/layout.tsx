import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Snake Game - Portfolio Project",
  description:
    "A modern web-based implementation of the classic Snake game built with Next.js, featuring persistent leaderboard functionality and optimized for portfolio demonstration.",
  keywords: [
    "snake game",
    "next.js",
    "react",
    "typescript",
    "portfolio",
    "web game",
  ],
  authors: [{ name: "Developer Portfolio" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
